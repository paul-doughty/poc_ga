/**
 * This component will basically just return the children it has
 * Used to wrap up components within a class
 * @param {Object} props 
 */
const aux = (props) => props.children;

export default aux;